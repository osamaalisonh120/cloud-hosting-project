import { Comment } from "@prisma/client";

export async function getAllComments(token: string): Promise<Comment[]> {
  const res = await fetch(`$/api/comments`, {
    headers: {
      Cookie: `jwtToken=${token}` // 👈 لازم C كبيرة
    },
    cache: "no-store",
  
  });

  if (!res.ok) {
    throw new Error("Failed to fetch data");
  }

  return res.json();
}




//  (قيمته)  التوكن حط
//  jwtToken” اسمها كوكي  في

// 🧠 البسيط  بالعربي  المعنى

//  الطلب ببعت وأنا
//  الكرتونة فوق  اللي الورقة في بحط (headers)

//  بتقول معلومة
// الجيب  في  بسكويت  ان 
//  jwtToken اسمه
//  التوكن وجواه


//  الكوكيز   في  JWT Token بتبعت دي الدالة
// للسيرفر، والسيرفر يتحقق من المستخدم
// ولو مصرح له → يرجّع كل التعليقات.

// ┌──────────────────────────┐
// │ استدعاء الدالة           │
// │ getAllComments(token)    │
// └─────────────┬────────────┘
//               │
//               ▼
// ┌──────────────────────────┐
// │ تجهيز Request            │
// │ URL = DOMAIN/api/comments│
// └─────────────┬────────────┘
//               │
//               ▼
// ┌──────────────────────────┐
// │ إضافة Header              │
// │ Cookie: jwtToken=token   │
// └─────────────┬────────────┘
//               │
//               ▼
// ┌──────────────────────────┐
// │ إرسال Fetch (GET)        │
// │ await fetch(...)         │
// └─────────────┬────────────┘
//               │
//               ▼
// ┌──────────────────────────┐
// │ هل response.ok ؟         │
// └─────────────┬────────────┘
//         ┌─────┴─────┐
//         │           │
//        لا           نعم
//         │           │
//         ▼           ▼
// ┌────────────────┐  ┌──────────────────────────┐
// │ throw Error     │  │ تحويل response إلى JSON │
// │ Failed to fetch │  │ response.json()         │
// └────────────────┘  └─────────────┬────────────┘
//                                    │
//                                    ▼
//                          ┌────────────────────┐
//                          │ Comment[]           │
//                          │ (كل التعليقات)     │
//                          └─────────┬──────────┘
//                                    │
//                                    ▼
//                                 return
